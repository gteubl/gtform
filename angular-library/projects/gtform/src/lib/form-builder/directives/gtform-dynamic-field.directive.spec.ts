import { GtformDynamicFieldDirective } from './gtform-dynamic-field.directive';

describe('GtformDynamicFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new GtformDynamicFieldDirective();
    expect(directive).toBeTruthy();
  });
});
